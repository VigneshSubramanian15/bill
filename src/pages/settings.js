import { Settings } from "@/Components/UI/Settings/index";
// import { Settings } from "@/Components/UI/Settings";
import { Layout } from "@/Components/Util/Layout";

export default function SettingPage() {
    return (
        <Layout>
            <Settings />
        </Layout>
    );
}
